# bug-<NNNN>: <slug>

**Status:** OPEN | IN-PROGRESS | FIXED | RECURRED | WONTFIX
**First seen:** <YYYY-MM-DD>
**Component:** <docs/components/<component> this touches, if known>

## Symptom

<What the user observes. Exact, reproducible if possible: what they do, what happens,
what should happen.>

## Context / root cause

<What we understand about why it happens. The surface, the code path, the suspected
root cause. Link the component spec / UXI it violates.>

## Planned solution

<The approach BEFORE implementing. What we intend to change and why it should work.>

## Approaches already tried (do NOT repeat)

<Running list of fixes attempted that did NOT hold — so a future attempt doesn't
circle back to them. Filled from the log below over time.>

- <none yet>

---

## Log

### <YYYY-MM-DD HH:MM> — <what was done>

<The actual implementation this time: what changed (files/commits), how it was
verified (test name + negative control), and the outcome. If it later recurs, append
a NEW dated entry below — never rewrite an old one. Be faithful: record what failed.>
