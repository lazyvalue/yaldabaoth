# Bug Manifest

The index of every bug we've worked on. **Read this first** (via `/bug`) before
attacking a bug — if it's here, the linked file holds the history of what was already
tried, so we don't attempt the same failed fix twice (that's the definition of
insanity). Higher fidelity than git: each bug file carries context, the planned fix,
and a **timestamped log** of every actual attempt.

## How this works

- Each bug is `docs/bugs/bug-<NNNN>-<slug>.md` (zero-padded, sequential). Template:
  `docs/bugs/_template.md`.
- A bug addressed **more than once** does NOT get a new file — it gets a new
  timestamped entry appended to the bottom of its existing file. One bug, one file,
  a growing log.
- Status: `OPEN` (known, not fixed) · `IN-PROGRESS` · `FIXED` (fix landed +
  verified) · `RECURRED` (came back after a FIXED — see its latest log entry) ·
  `WONTFIX`.

## Bugs

| Id | Slug | Status | First seen | Times addressed | One-line |
|----|------|--------|------------|-----------------|----------|
| bug-0001 | created-session-not-persisted | RECURRED→FIXED | 2026-07-14 | 3 | created server-managed sessions weren't persisted (resume_id/channel both None) → picker on restart; fixed by resolving the sid via the store's `sid_of` |
| bug-0002 | restore-drops-replayed-history | RECURRED→FIXED | 2026-07-15 | 2 | eager generation rebaseline still emptied restored tiles because the server correctly fenced duplicate ACP replay after `ChannelOpened`; generation bumps now reset only generation-scoped ledgers and preserve the WAL-built transcript |
| bug-0003 | transcript-selection-cursor-line-no-token-hits | FIXED | 2026-07-15 | 1 | transcript mouse-select copied the wrong text when the transcript was focused: the caret line rendered via the caret-injection path which registered NO hit-test tokens, so a mouse-down there snapped the anchor to the nearest other line; fixed by routing every emitted piece (incl. caret cell + empty lines) through `register_token_on_paint` in `build_wrapped_line` |
| bug-0004 | two-adjacent-you-blocks | FIXED | 2026-07-16 | 1 | two You-blocks rendered adjacent (next to each other) when the blank line between their anchors collapsed — `open_you_block_at_cursor` keyed on raw anchor equality, not render slot; fixed with `you_blocks_would_be_adjacent` (all-blank-between ⇒ same slot ⇒ resume, don't spawn); separated multi-insertion preserved |
| bug-0005 | duplicate-claude-labels-on-restore | FIXED | 2026-07-16 | 1 | after GUI restore, two sessions in one cwd both named bare 'claude' — three fallbacks default to bare "claude" and the restore loop never dedupes labels; fixed with `unique_label`/`dedupe_slot_labels` in `load_persisted_acp_sessions` + running dedup in `restore_agent_leaves` (missing label → numbered claude-N) |
| bug-0006 | transcript-select-copies-wrong-span-on-stripped-lines | FIXED | 2026-07-16 | 1 | transcript drag-select copied a shifted raw slice (`:** scott+…`) on markdown lines — `build_wrapped_line` registered `TokenHit.start_char` in STRIPPED render space but the editor/`selection_text` slice RAW; fixed by registering raw offsets via `stripped_to_raw_cols` (+ raw→rendered band conversion). Localized by a Fable subagent |
| bug-0007 | jump-panel-sessions-spontaneously-reorder | RECURRED→FIXED | 2026-07-16 | 2 | jump-panel agent sessions reorder on their own. (1) 2026-07-16: local-only rows were APPENDED after the label-sorted roster rows and hopped on roster catch-up — fixed by sorting the combined list. (2) 2026-07-21 RECURRENCE: a session sank to the BOTTOM after `/clear` — the user's drag order ranks by server sid and `/clear` mints a NEW sid (unranked ⇒ `usize::MAX`); fixed with an explicit order succession (`jump_order_succession` + `AgentRow::order_sid` + `inherit_order_slot`) so the row keeps its slot mid-open and the fresh sid replaces the old one in place |
| bug-0008 | cannot-select-parsed-blocks-in-transcript | FIXED | 2026-07-16 | 1 | can't mouse-select tables / bullet lists / code in the transcript — `FlatItem::Block` renders via `block_inner` whose `RenderCtx` has NO token-hit sink, so parsed blocks register zero `TokenHit`s; fixed with `register_block_hits_on_paint` (per-line bands for lists/code, PER-CELL bands for tables via `parse_table_cell_ranges`); runtime-unverified |
| bug-0009 | camera-pan-does-not-snap-to-slots | FIXED | 2026-07-17 | 1 | Cmd+Shift free-pan rests on a fractional slot — `desktop_drop`'s `pan_drag` branch was the one branch that saved+returned WITHOUT `snap_camera_to_slots()` (UXI-Workspace-8 left free-pan out of scope). Fixed by snapping on pan release; UXI-Workspace-8 reconciled to include free-pan |
| bug-0010 | transcript-selection-grows-on-streamed-text | FIXED | 2026-07-17 | 1 | agent-streamed text auto-selects — transcript `selection_range = anchor..cursor` with an auto-advancing caret; `splice_insert` shifts the cursor but not the anchor, and `move_cursor_to_tail` jumps the caret without clearing the anchor, so a persisted click/select balloons over new content. Fix: shift the anchor in splice + collapse selection on the tail-jump |
| bug-0012 | new-tile-placed-diagonally | FIXED | 2026-07-21 | 1 | a new tile in a one-tile workspace landed diagonally ("up and to the right") — `seed_slot` always spiraled from the ORIGIN (ignoring where the existing tile sits) and scanned each ring in raw row-major order (ring 1 entered at its top-left, `(-1,-1)`). Fixed with `seed_slot_near`/`reconcile_near`: spiral centered on the focused/last-revealed tile, ring scanned in preference order (same row → right → below), so the new tile lands at `(row, col+1)` |
| bug-0011 | agent-picker-eats-workspace-nav-keys | FIXED | 2026-07-20 | 1 | the session selector/picker (unbound agent tile, `render_agent_picker`) was the one screen root missing `.workspace_nav(cx)`, so while it was focused `ctrl-<n>` / `cmd-shift-[]` bubbled into a dead chain (no window-level fallback) — the picker "ate" workspace-switch keys. Fixed by wiring `workspace_nav` + `toggle_jump_panel` onto the `AgentPickerView` root; guard forces a repaint so the picker is the focused dispatch node (a first guard false-passed because `set_screen` doesn't notify) |

| bug-0013 | tool-call-still-splits-agent-sentence | FIXED | 2026-07-21 | 1 | tool rows still guillotined agent prose mid-sentence — the `dbe67be` UXI-AgentTile-8 fix only rejoined when BOTH sides of the break were alphanumeric, so a continuation starting with a space ("…the fix for" | Bash | " it on my side…") or bare punctuation (a stranded lone `.` line) still split; fixed by asking of the pre-tool text alone "did the model finish a sentence?" (`continuation_rejoin_point`) |
| bug-0014 | yaml-frontmatter-renders-as-giant-heading | FIXED | 2026-07-21 | 1 | markdown files with YAML frontmatter rendered the whole frontmatter as one enormous run-on `<h2>` — nothing in the pipeline knew what frontmatter was, so CommonMark read the closing `---` as a setext underline promoting the metadata paragraph to a heading; fixed by enabling pulldown-cmark's `ENABLE_YAML_STYLE_METADATA_BLOCKS` + a new de-emphasized `RenderedBlock::Metadata` |
| bug-0015 | code-block-shifts-under-the-pointer-on-click | RECURRED→FIXED+SHIPPED | 2026-07-21 | 2 | "still can't select in a multiline code block" — clicking inside a block moved the editor cursor, collapsing the cursor-kept blank line, dropping a list item and repainting the block +25px under the pointer mid-drag so the drag grabbed the wrong lines. The `drag_protect_line` freeze was correct + PAINT-guarded (+25px NC) but STRANDED UNCOMMITTED — the user runs `main` release and never had it (anti-circling rule 5; HEAD didn't even compile: guard committed without the field). 2026-07-22: committed the fix to main + rebuilt the binary + promoted to `UXI-Selection-2` |
| bug-0017 | cannot-select-code-blocks-in-worksheet | FIXED | 2026-07-22 | 1 | can't mouse-select code blocks in the worksheet transcript — a code block is one `FlatItem::Block` that (1) NEVER painted a selection highlight (`doc_selection: None`, so model+clipboard worked but the user saw nothing — why bug-0008/0015 "fixes" never changed anything) and (2) registered EVEN-SPLIT hit bands over the FENCE-INCLUSIVE raw range offset by the block's padding/`[lang]` header, so clicks mapped a line off. Fixed by routing code-block content lines through per-line REAL-bounds `TokenHit`s + run-bg selection painting keyed by raw line (`RenderCtx::block_hits`, `raw_base = start+1`); pair blocks against parsed-only ranges. `UXI-Selection-3` |
| bug-0018 | external-link-does-not-open-browser | RECURRED→FIXED | 2026-07-23 | 2 | links worked in rendered documents but remained inert in Agent Tile transcripts, whose highlighter stripped Markdown destinations before the document-view click wiring; fixed by restoring link ranges on committed agent lines and routing their real painted click through the existing local/external dispatcher |
| bug-0019 | project-context-menu-items-do-nothing | FIXED | 2026-07-24 | 1 | clicking any item in the jump-panel project context menu did nothing (accelerators `w`/`a`/`d` worked) — the popup didn't `.occlude()`, so GPUI's hit test (which collects EVERY hitbox under the pointer and stops only at a `BlockMouse` one) left the full-window click-away backdrop hovered underneath; the press fired the backdrop's `on_mouse_down` → `clear_overlay()`, and `on_click` (down-then-up on the same element) never fired. Fixed with `.occlude()` on the popup + a guard that `simulate_click`s the item's REAL painted rect (the old guard called `project_menu_action` directly — the hand-built proxy anti-circling rule 1 warns about) |
| bug-0016 | renamed-session-names-lost-on-restart | FIXED | 2026-07-22 | 1 | renamed sessions reverted to `claude-N` after exit — `acp_session_persist_path()` (where session LABELS live) was the ONE persistence path NOT guarded under `cfg(test)`; it fell through to real `~/.yalda/acp_sessions.json`, so every view-booting test run (`save_agent_ring` from clear/restore/rename) overwrote the user's labels with `{S2/claude-2}` fixtures. Fixed by returning `None` in tests w/o override (matching workspace/preferences paths) + `recover_labels_from_roster` adopting the WAL-backed roster label for auto-named opened sessions (gated by `is_auto_claude_label`) so lost names come back and future clobbers are harmless |
| bug-0020 | autoname-summary-lost-on-reload | FIXED | 2026-07-24 | 1 | the Haiku autoname SUMMARY under a jump-panel session vanished after a GUI reload — its only durable home was `acp_sessions.json`, which is cwd-keyed and holds only TILE-BOUND sessions (the panel lists every server session), and `jump_panel_agent_rows` read it only off a live in-store entity; the naming call is one-shot so a lost summary never comes back. Fixed with an id-keyed sidecar (`session_summaries.json`) written at `finish_autoname` and used as the fallback in both row builders + restore |
| bug-0021 | autoname-never-fires-for-most-sessions | FIXED | 2026-07-25 | 1 | the Haiku namer never fired for most sessions — the arm was keyed on PROVENANCE (`armed_for_autoname` at fresh-create constructors), which misses jump-panel-created free sessions (server-side only, no local state to arm), `/clear`, and anything restored; the empty-transcript drain branch also burned the one-shot before replay landed. Fixed by arming on IDENTITY at every sid bind (`maybe_arm_autoname`: still `claude-N` + not user-renamed + one-shot unspent for this sid, recorded durably) + `finish_replay` marking it due |
| bug-0022 | status-marks-only-for-open-sessions | FIXED | 2026-07-25 | 1 | working/waiting marks appeared only for sessions this GUI had OPEN — `awaiting`/`unread` were read solely off a live in-store entity while the panel lists the whole roster, so free/other-GUI sessions were permanently Neutral (per-session ReplyEvents only reach subscribers). Fixed server-side: `SessionInfo.busy` (serde-default) + a global `SessionBusy` broadcast, with a roster-side unread mark for busy→idle while you're elsewhere. NEEDS A SERVER RESTART to take effect |
| bug-0023 | cannot-expand-folded-tool-blocks | FIXED | 2026-07-26 | 1 | folded tool-use blocks could no longer be expanded — `render_agent` keys the cached transcript's wrapper element id on the render fingerprint (the UXI-AgentTile-7 dropped-notify backstop), and that id is an ANCESTOR of the whole transcript, so the press itself (which sets `cursor_line`/`cursor_col`/`transcript_focused`, all fingerprint fields) re-keyed the header's element state between down and up and gpui's `on_click` never fired — for EVERY clickable thing in the transcript. Fixed by freezing the id fingerprint at its pre-press value for the gesture (`TranscriptView::element_fp_freeze`, bug-0015's `drag_protect_line` shape); shipped with UXI-AgentTile-29's `j`/`k` hop over tool-anchor lines |
| bug-0024 | codex-restores-as-claude | FIXED | 2026-07-28 | 1 | restored Codex tiles showed Claude because synchronous workspace restore raced the async roster seed and permanently kept the default provider; fixed by persisting provider per local session plus reconciling open session identity from the WAL-backed roster, with real painted tile/transcript regression coverage |
| bug-0025 | autoname-request-stuck-without-key | FIXED | 2026-07-28 | 1 | when `ANTHROPIC_API_KEY` was absent, the autoname worker returned without settling, leaving the session permanently `Requested` with no summary; fixed by an eight-second bound, visible pending state, and a persisted opening-topic fallback for missing credentials, failures, and omitted summaries |
| bug-0026 | archived-session-visible-in-waiting | FIXED | 2026-07-28 | 1 | live GUI was older than the release executable on disk; current Waiting projection already excludes archived sids, now locked by a real right-click → repaint regression guard and shipped through a clean release restart |
| bug-0027 | sessions-stuck-showing-unavailable | FIXED | 2026-07-31 | 1 | sessions read `Unavailable` in the jump panel while demonstrably alive (the reporter's own mid-turn session) — the exact residue of bug-0022: that fix broadcast `busy` roster-wide but left `connected` with NO live source at all. `SessionCreated` necessarily carries `connected: false` (it precedes the blocking spawn handshake), `PublishChannel` publishes only `busy`, and `refresh_roster` is a seed not a poll — so the flag froze at the last full `list_sessions` for the rest of the session's life. Fixed server-side: `Notification::SessionConnected` + `broadcast_connected` from PublishChannel/AgentDisconnected/SpawnFailed, with `AgentRoster::set_connected` + a reducer arm. Do NOT "fix" by polling. NEEDS A SERVER RESTART to take effect |
| bug-0029 | model-badge-click-opens-nothing | FIXED | 2026-08-04 | 1 | clicking the status-strip `model ▾` badge presented NOTHING (the `▾` was there, so the picklist had arrived) — the badge dispatches `OpenLocalMenu`, and a dispatched action only reaches a listener on the FOCUSED node's path, but `on_action(Self::open_local_menu)` was registered ONLY on the `YaldaView` and `BrowserView` roots; the `AgentView` root registers ~50 actions and not that one, so the action walked the tree and was dropped. The keyboard half worked because `space` never uses the action (`leader_intercept` calls `open_local_menu_inner` directly). Fixed by registering the action on the `AgentView` root; guard clicks the badge's REAL painted rect |
| bug-0030 | cannot-highlight-table-cell-selection | FIXED | 2026-08-06 | 1 | can't SEE a highlight when selecting a transcript TABLE CELL (copy worked, no paint) — third face of the parsed-`FlatItem::Block` family: bug-0017 painted code-block selection but tables stayed on the even-split band path that registers hits yet paints nothing (`doc_selection: None`). Fixed by painting a selection QUAD in the same band geometry the hits use (uniform-width model, consistent by construction) in `register_block_hits_on_paint`; guard asserts `DocRenderTap.band_selection`. Bullet lists were investigated + are prose Lines (already highlight) — not a Block, out of scope |
| bug-0031 | charselect-caret-selection-mismatch | FIXED | 2026-08-07 | 1 | in char-select mode (`v`+motion) the caret block sat one cell PAST the selection highlight — the selection model is exclusive of the head cell (`[start, cursor)` for both highlight and copy) but the Normal-mode caret draws a block OVER the cell at `cursor_col`. Fixed glyph-only: `caret_mode_during_selection` renders a BEAM (Insert-style, flush at the selection edge) while a selection is active; copy/selection ranges untouched. Guard asserts the painted caret mode via `DocRenderTap.caret_beam_on_cursor_line` |
| bug-0034 | list-wrap-fails-in-buffer-tiles | FIXED | 2026-08-06 | 3 | "word wrapping often fails on bullet points / lists" in a Buffer tile — the rendered doc view is fine (char-wraps via StyledText), but the Code/WP EDIT views (and worksheet transcript) go through `build_wrapped_line`, whose whitespace-token `flex_wrap` can only break BETWEEN children — so a single unbreakable run (a path/URL in a bullet) overflowed and was CLIPPED by `overflow_x_hidden`, hiding its tail. Fixed with `chunk_overlong_tokens` (split runs >40 chars into 16-char chunks; offsets abut so caret/selection/hit-test unchanged). Guard `code_edit_wraps_unbroken_token_in_bullet` (NC RED = 1-line clip); UXI-TextEditing-4. Separate unconfirmed thread: narrow-split Auto-sizing collapse (needs a live read) |
| bug-0032 | transcript-nav-auto-highlights | FIXED | 2026-08-07 | 1 | keyboard nav in the worksheet transcript auto-highlighted text — `V`/`v` turn `extend_mode` ON but the transcript's own `Esc` branch (`agent_ui.rs`) returned before `dispatch_normal_core`'s Esc-clears-extend-mode, so it stuck ON with no exit and every motion extended. Fixed: transcript `Esc` cancels an active selection + exits extend mode; `reply_quote_at_cursor` also clears it. Guards: `worksheet_esc_exits_extend_mode_stops_autohighlight` + `worksheet_reply_clears_extend_mode` |
| bug-0033 | code-fence-bleeds-across-transcript | FIXED | 2026-08-07 | 1 | a stray/unclosed ` or ``` bled and rendered the REST of the transcript as code — a fence was parsed across the WHOLE document, not one agent turn. Two carriers: `highlight_cache.rs` folded one `FenceState` over all lines (later turns got code_bg), and `detect_block_ranges` searched to EOF for the close (stray ``` paired with a later turn's ``` into one giant block). Fixed by bounding the fence to the opening line's frozen range: highlighter resets FenceState at every frozen-span boundary; detector caps the close search at the turn end. Guards: `fence_resets_at_frozen_turn_boundary_no_bleed` + `detect_block_ranges_bounds_fence_to_turn` |
| bug-0035 | buffer-reload-syntax-state-bleeds | FIXED | 2026-08-11 | 1 | reloading a Buffer could keep painting its previous text and syntax state because whole-core replacement reset `edit_seq` to zero, aliasing the edit view's generation-keyed line/highlight caches; fixed at the shared core replacement boundary by advancing the generation across reload |
| bug-0036 | codex-message-does-not-interrupt | FIXED | 2026-08-12 | 2 | successive Codex questions and explicit Stop now use the adapter's advertised native FIFO steering: the initial prompt, each steer, and Stop share one ordered worker-control stream, preventing overtakes and drained-cancel races; older adapters retain cancel-then-prompt compatibility. Exact GUI sequence, real subprocess wire order, legacy fallback, full suites, release builds, and mutation controls passed |
| bug-0037 | agent-v-selection-stops-mid-line | FIXED | 2026-08-13 | 1 | Agent Tile `V` selected the current whole line only on the first frame; `j`/`k` then reused characterwise sticky-column motion and stopped mid-line. Fixed with a distinct `EditorView` linewise extend state that normalizes every motion to full logical-line endpoints; real worksheet `V j`/`V k` guards use unequal line lengths |
| bug-0038 | filter-keys-open-file-prematurely | FIXED | 2026-08-16 | 1 | in a Buffer file picker's `/` search, certain keys opened the selected file / navigated away instead of editing the query — the `/`-filter text was accumulated by a CAPTURE-phase key listener whose `stop_propagation` was assumed to suppress the `BrowserView` action bindings, but GPUI 0.2.2 dispatches bound actions BEFORE capture listeners and consumes the event, so every bound key (`right`/`l`→BrowserEnter opens, `left`/`h`/`-`→BrowserParent, `r`→rename, …) fired its action mid-search. Fixed by rendering the browser/rail under a distinct key context (`BrowserFilter`/`RailFilter`, no bare-letter bindings) while filtering, so bound keys never match a binding and are typed into the query by the capture handler; nav/open/mutate action handlers also guard on `browser_text_captured()` as defense-in-depth. Both faces (buffer picker + rail) covered by real-keymap guard tests with RED negative controls |
| bug-0028 | archive-tears-down-the-gui-connection | FIXED | 2026-08-04 | 1 | "unarchive has trouble starting up again" — archiving ONE session tore down the GUI's WHOLE session-server socket. `do_set_archived` released the forwarder via `ForwarderHandle::evicted`, but `evicted` is the high-water KILL flag whose handler does `writer.lock().await.shutdown()` on the PER-CONNECTION write half (`stream.into_split()`, cloned to every session forwarder) — so every attached session dropped and reconnected from base; unarchive is just when it became visible. Likely the reconnect-storm source too. Fixed with a distinct `released` flag + pure `forwarder_stop_action` mapping (ThisSessionOnly vs ShutdownConnection). Do NOT force-restart the session to "fix" it; the respawn was never broken. NEEDS A SERVER RESTART to take effect |
| bug-0039 | image-paste-dropped-on-macos | FIXED | 2026-08-16 | 1 | Cmd+V of an image into an agent session pasted nothing usable (no chip; a URL/filename or empty compose) — INV-UX-21's one untested assumption. GPUI 0.2.2's mac `read_from_clipboard` (`platform/mac/platform.rs:1102-1137`) checks for a string type FIRST and returns a string-only `ClipboardItem` whenever the board carries any `public.utf8-plain-text` — never reaching its image branch. mac image copies (browsers, Finder file-copy, most apps) put a URL/filename rep ALONGSIDE the image (confirmed: board advertised `public.png`+`public.tiff`+`public.utf8-plain-text` together), so the image was dropped; only a pure screenshot (no string) worked. Fixed by reading the general `NSPasteboard` directly (`system_console::read_clipboard_image_png`, PNG then TIFF→PNG via NSBitmapImageRep) BEFORE GPUI, which stays the non-mac fallback; `paste_into_compose` stages the direct read as `image/png`. Guards: pure `select_clipboard_png_*`, headless `image_paste_direct_read_stages_even_with_text_on_clipboard` (real path, text-only GPUI clipboard + injected read), `#[ignore]` `read_clipboard_image_png_os_recovers_png_beside_text` (real-pasteboard round-trip, gap-2) — all NC RED. Live `claude-agent-acp` `image` capability still the separate NEEDS-RUNTIME |
| bug-0040 | worksheet-reply-wraps-narrow-first-paint | FIXED | 2026-08-17 | 1 | a newly seeded `r` reply sometimes wrapped at a strange ~40-column width in a wide Agent Tile — the inline You-block rendered before its `CaptureBounds` cell was populated, took the literal 40-column fallback, and the cached transcript had no reason to render again after paint. Fixed by deriving the unmeasured first-paint columns from the already-painted transcript viewport while preserving exact compose bounds once available. Painted real-path guard: `worksheet_r_first_paint_uses_transcript_width`; NC RED = 1574px viewport / 142px-tall narrow reply |
| bug-0041 | inline-tool-detail-utf8-panic | FIXED | 2026-08-17 | 1 | an emoji crossing the 60-byte inline Bash-command limit panicked the main GPUI thread because `tool_inline_detail` sliced UTF-8 text by bytes; fixed command and search details with character-boundary-safe truncation. Exact reported command guard; NC RED reproduced byte index 60 inside `🖼`; 2/2 targeted mutants caught |
| bug-0042 | agent-picker-loses-workspace-placement | RECURRENT→FIXED | 2026-08-19 | 2 | selecting an existing Agent from a workspace picker first created a temporary competing tile, then the first fix left a live-roster keyboard gap: rendering clamped a stale selection after rows shrank but Enter submitted the old out-of-range index and did nothing. Fixed stable-tile replacement plus keyboard cursor normalization against the current projection; real click/Enter, already-local, tagged live-shrink, command, and typography guards |
| bug-0043 | jump-tag-items-oversized | FIXED | 2026-08-19 | 1 | tile-native Unbound tag folders inherited a larger fallback; production headers now pin the compact monospace subheader typography and a painted geometry guard proves tagged/loose rows remain fixed through document zoom |
| bug-0044 | unbound-picker-tiles-cannot-close | FIXED | 2026-08-19 | 1 | `Frame::close_focused` rejected every direct-Unbound focus; Close Tile now removes the exact stable Unbound tile, prunes scratchpad references, clears direct focus, and reveals the active workspace, with real Buffer/Agent picker command coverage |
| bug-0045 | ctrl-w-missing-by-tile-state | RECURRENT→FIXED | 2026-08-19 | 2 | Original missing-listener bug remains fixed; layout-aware focus now follows Plane spatial geometry, Columns/Tiling visible horizontal order, and deterministic Monocle traversal instead of always consulting hidden Plane coordinates |
| bug-0046 | close-bound-agent-drops-unbound-entry | FIXED | 2026-08-19 | 1 | Close Tile now routes keyboard/menu through one exhaustive transition; bound Agents move intact to Unbound + scratchpad, including the sole-workspace floor case |
| bug-0047 | agent-session-duplicated-across-projects | FIXED | 2026-08-19 | 1 | the bind choke retires racing roster duplicates; restore chooses the canonical tile by authoritative session cwd/project and rewrites healed state; persistence rejects duplicate identity graphs |
| bug-0048 | send-workspace-picker-content-label | FIXED | 2026-08-19 | 1 | Send tile destinations now use stable workspace names only; the compact centered picker has explicit Current/Create hierarchy, fixed chrome, clickable rows, and painted regression coverage |
| bug-0049 | active-workspace-label-low-contrast | FIXED | 2026-08-19 | 1 | active workspace names retain foreground contrast while a neutral background and narrow accent rail carry selection |
| bug-0050 | jump-tile-name-wraps | FIXED | 2026-08-19 | 1 | primary jump-panel names are single-line ellipsizing labels with stable row geometry |
| bug-0051 | hidden-tile-has-no-indicator | FIXED | 2026-08-19 | 1 | typed tile placement now paints a compact hidden marker for every hidden Agent and non-Agent tile |
| bug-0052 | jump-workspace-cards-collapse-to-bands | FIXED | 2026-08-19 | 1 | bounded workspace groups are non-shrinking scroll items; crowded expanded/collapsed paint coverage prevents 4px clipped blue bands |
| bug-0053 | recovered-sessions-drop-live-replies | FIXED | 2026-08-22 | 1 | whole-server recovery reset live channels to generation 0 even when retained WAL history had already reached generation 1, so the GUI discarded completed live replies as stale and remained Thinking; recovery now seeds max durable generation + 1 and resets per-generation seq to 0; shipped on main and runtime-verified across all 21 live sessions |
| bug-0054 | terminal-session-stays-thinking | FIXED | 2026-08-22 | 1 | disconnect, spawn failure, cancel, detach, and prompt rejection paths could leave queued work or the GUI turn phase stuck Thinking; lifecycle states now terminalize busy/pending work and the GUI settles to Idle |
| bug-0055 | timed-out-session-request-leaks-waiter | FIXED | 2026-08-22 | 1 | timed-out or failed session-client RPCs left pending response senders registered forever, retaining memory and accepting misleading late replies; request cleanup is now centralized and deterministic |
| bug-0056 | session-settings-and-prompts-not-durable | FIXED | 2026-08-22 | 1 | permission/model changes and prompts admitted during spawn were memory-only, so a daemon crash could revert safety state or silently lose queued text/images; WAL metadata and prompt intent/terminal records now recover exact unsettled state |
| bug-0057 | session-restart-close-attach-races | FIXED | 2026-08-22 | 1 | restart publication, repeated unarchive, duplicate same-connection attach, spawn-thread failure, and close deletion ordering admitted stale channels, stuck retries, duplicate forwarders, or closed-session resurrection; transitions are now generation-fenced and failure-aware |
| bug-0058 | gui-restart-leaves-stale-windows | FIXED | 2026-08-22 | 1 | `dev-gui.sh` launched a replacement after a fixed 300ms delay even when the old GPUI ignored TERM, leaving stale empty windows and two clients; restart now waits, exact-pattern KILLs only repo-built GUIs, verifies exit, then launches exactly one replacement |
| bug-0059 | reboot-detaches-agent-tile | FIXED | 2026-08-25 | 1 | GUI boot now restores `workspace.json` before starting universal roster reconciliation, preventing a fast roster result from persisting the pre-restore frame over named-workspace Agent membership; exact production-initializer guard, RED ordering control, full suite, and mutation gate passed |
<!-- Example row once populated:
| bug-0001 | chatbox-caret-offscreen | RECURRED | 2026-05-02 | 16 | caret + text scroll out of the visible chatbox; no single owner of caret-in-viewport |
-->
